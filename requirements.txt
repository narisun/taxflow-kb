# ─────────────────────────────────────────────────────────────────────────────
# TaxFlow AI — Python Dependencies
# ─────────────────────────────────────────────────────────────────────────────
# Single requirements file for the entire monorepo. Organized by subsystem.
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Core / Shared ────────────────────────────────────────────────────────────
pydantic[email]>=2.10
pydantic-settings>=2.6

# ── FastAPI Backend (api/) ───────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
alembic>=1.14
python-multipart>=0.0.12
sse-starlette>=2.0

# Auth (JWT validation, JWKS caching)
python-jose[cryptography]>=3.3
cachetools>=5.3

# PII encryption
cryptography>=43.0

# Document extraction (Claude vision)
anthropic>=0.40

# PDF generation & text extraction
pypdf>=4.0
pdfplumber>=0.10

# UUIDv7 (RFC 9562) — time-ordered, globally unique primary keys
uuid6>=2024.7

# MCP tool registration for the agentic layer
fastmcp>=2.0

# ── Knowledge Base / RAG (taxkb/) ────────────────────────────────────────
# Graph database
neo4j>=5.0

# Sync Postgres driver (ingestion pipelines)
psycopg2-binary>=2.9

# OpenAI embedding & synthesis
openai>=1.0

# Token counting (cl100k_base, matches text-embedding-3-small/large)
tiktoken>=0.6

# pgvector adapter for semantic search
pgvector>=0.2
numpy>=1.24

# ── Dev / Test ───────────────────────────────────────────────────────────────
pytest>=7.4
pytest-asyncio>=0.24
pytest-cov>=5.0
pytest-timeout>=2.3
